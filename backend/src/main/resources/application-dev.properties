# MySQL (LOCAL)
spring.datasource.url=jdbc:mysql://localhost:3306/student_task_manager?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.username=root
spring.datasource.password=kritika

spring.jpa.hibernate.ddl-auto=update
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# JWT (same as before)
app.jwt.secret=StudentTaskManagerSecretKey2024SuperSecretKeyForJWTTokenGeneration
app.jwt.expiration=86400000

# CORS (local)
app.cors.allowed-origins=http://localhost:5173

# File upload (same)
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB